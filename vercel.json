{
  "version": 2,
  "installCommand": "npm install --prefix server",
  "buildCommand": "npm install --prefix server",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
